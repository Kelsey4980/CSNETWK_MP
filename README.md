# CSNETWK_MP
### How to Run
1. Run `python webserver.py` on terminal.
2. On a different terminal, go to `test_send` folder. Run `python <test_file.py>`.
3. Check the server terminal if the message is received.